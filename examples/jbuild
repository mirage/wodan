(jbuild_version 1)

(executables
  ((names (irmin_example irmin_cli))
  (libraries (checkseum.c wodan io-page-unix mirage-block-unix mirage-block-ramdisk nocrypto.lwt
  cmdliner irmin-mem irmin-unix
  logs.fmt logs.cli fmt.cli fmt.tty
  ))
  ))
